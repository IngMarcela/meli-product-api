version: 2.1

executors:
  go-executor:
    docker:
      - image: cimg/go:1.21 # Usá tu versión de Go
    working_directory: ~/repo

jobs:
  test:
    executor: go-executor
    steps:
      - checkout
      - run: go mod tidy
      - run: go test ./...

workflows:
  test:
    jobs:
      - test
